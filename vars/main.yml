_cloud:
  provider: "{{ cloud_provider | default({}) }}"
  storage:  "{{ cloud_storage  | default({}) }}"
  network:  "{{ cloud_network  | default({}) }}"
  compute:  "{{ cloud_compute  | default({}) }}"

_cloud_facts:
  compute: {}
  network: {}
  storage: {}
